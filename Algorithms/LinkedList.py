# import LinkedList


# linked_list = LinkedList()

# print(linked_list)

# def isValid(s):
#     list = []
#     dict = { ")":"(", "]":"[", "}":"{" }

#     for char in tuple(s):
#         if char in dict:
#             if not list or not dict[char] == list.pop():
#                 return False
#         else:
#             list.append(char)
            
#     return not list

# print(isValid("({{{{}}}))"))
# print(isValid("(])"))
# print(isValid("()"))

original_list = [1, 4, 6, 2, 2, 1, 5, 3, 2]


# print(original_list.index(5, original_list.index(5) + 1))

print(3 / 2)

# print([v for k, v in enumerate(original_list) if v > 1])